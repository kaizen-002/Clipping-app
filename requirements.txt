# Pinned. rules.md: virtual environment plus a lock file.
pydantic==2.9.2
fastapi==0.115.0
uvicorn==0.30.6
httpx==0.27.2
faster-whisper==1.0.3

# Deliberately NOT pinned. YouTube changes its extraction regularly and old
# yt-dlp releases start returning "HTTP Error 403: Forbidden". A pin here is a
# time bomb, not a guarantee of reproducibility. Update it when downloads fail:
#   bin/yt-dlp.exe -U     (bundled binary, used first)
#   pip install -U yt-dlp (fallback on PATH)
yt-dlp
